server:
  port: 8804


spring:
  application:
    name: applicationClient1

#注册中心地址，默认值是http://localhost:8761/eureka/，可省略
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8801/eureka/